%h4
	= form_for @new_user, url: users_path do |f|
		%p
			= f.label :name
			= f.text_field :name
		%p
			= f.label :email
			= f.email_field :email
		%p
			= f.label :username
			= f.text_field :username
		%p
			= f.submit :class => "button btn btn-primary"