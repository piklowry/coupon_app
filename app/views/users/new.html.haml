%h4
	= form_for @new_user, url: users_path do |f|
		%p
			= f.label :name
			= f.text_field :name
			%br
			= f.label :email
			= f.email_field :email
			%br
			= f.label :username
			= f.text_field :username
			%br
			= f.submit